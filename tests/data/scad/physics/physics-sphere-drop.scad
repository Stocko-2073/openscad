// Sphere dropped from a small height comes to rest with its center at ~r.
// Small drop keeps rolling minimal so the resting spot stays stable.
physics() translate([0, 0, 12]) sphere(r = 10, $fn = 48);
